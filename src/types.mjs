/**
 * @typedef ClassData
 * @property {string} id//（プロパティ名）科目の管理
 * @property {string} name//科目名
 * @typedef TableData
 * @property {string} dayperiod `<曜日>-<時間>`の形式のテキスト
 * @property {string} classId 科目のID
 */
